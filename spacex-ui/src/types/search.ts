/**
 * Represents a filter option for searching launch data.
 * 
 * @typedef {Object} SearchFilter
 * @property {string} name - The name of the filter.
 * @property {string} value - The value of the filter.
 */
export interface SearchFilter {
    name: string;
    value: string;
}